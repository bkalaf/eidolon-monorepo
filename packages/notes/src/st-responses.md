Alright — here are **copy/paste-ready prompts** for (A) the **square icon images** and (B) **3 Codex prompts** to generate the React components.

I’m going to force consistency by baking a shared “style contract” into every image prompt. Keep that identical so your icons look like a coherent UI set.

---

## A) Image prompts (square icons)

### Shared style contract (include this in every icon prompt)

Use this exact block at the end of every prompt:

> “Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.”

---

### Numbers 0–10 (11 icons)

[x] **ICON 0 (fiery zero ring)Depict the number zero as a perfect circular ring made of fire, so the ring itself reads as ‘0’. Flame tongues licking outward, glowing embers, bright core, darker smoke wisps kept minimal. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[x] **ICON 1 (die face 1 pip)Single die face representing the number 1: a square die face plate with one centered pip. Clean and readable at small size. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[x] **ICON 2 (die face 2 pips)Single die face representing the number 2: two pips arranged diagonally (top-left and bottom-right). Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[x] **ICON 3 (die face 3 pips)Single die face representing the number 3: three pips arranged diagonally (top-left, center, bottom-right). Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[x] **ICON 4 (die face 4 pips)Single die face representing the number 4: four pips at the corners. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[x] **ICON 5 (die face 5 pips)Single die face representing the number 5: four corner pips plus one center pip. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[x] **ICON 6 (die face 6 pips)Single die face representing the number 6: six pips in two vertical columns of three. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[ ] **ICON 7 (d10 with ‘7’)Ten-sided die (d10) viewed in a clear 3D isometric angle, showing the face value ‘7’ prominently. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[ ] **ICON 8 (d10 with ‘8’)Ten-sided die (d10) viewed in a clear 3D isometric angle, showing the face value ‘8’ prominently. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[ ] **ICON 9 (d10 with ‘9’)Ten-sided die (d10) viewed in a clear 3D isometric angle, showing the face value ‘9’ prominently. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[ ] **ICON 10 (d10 with ‘10’)Ten-sided die (d10) viewed in a clear 3D isometric angle, showing the face value ‘10’ prominently. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

---

### Yes / No (2 icons)

[ ] **ICON YES (thumbs up)Thumbs up icon, bold silhouette, friendly but not cartoony, centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[ ] **ICON NO (thumbs down)Thumbs down icon, bold silhouette, centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

---

### Unique “doesn’t duplicate anything else” icon (1 icon)

You asked for *an icon image that doesn't duplicate anything else* — I’m interpreting this as an **extra generic UI token** you can use as a wildcard/unknown.

[ ] **ICON UNKNOWN (mystery)A ‘mystery/unknown’ token icon: a question mark carved into a simple medallion shape, subtle cracks/engraving lines, centered. No other symbols. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

---

### Alignment tokens with text labels (5 icons)

These **must include text** exactly as you specified, so we rely on labels not vibes.

[ ] **ICON GOOD- Alignment token icon: a radiant sunburst or halo motif (clearly ‘good’), centered. Include the label text ‘GOOD’ in all caps within the design (but still clean and legible).”

* (Shared style contract, but override: text is allowed here)

[ ] **ICON SLIGHTLY GOOD- Alignment token icon: a smaller, softer sunburst/halo motif (less intense than GOOD), centered. Include the label text ‘SLIGHTLY GOOD’ in all caps within the design, legible.”

* (Shared style contract, override: text allowed)

[ ] **ICON NEUTRAL- Alignment token icon: balanced scales or a perfectly split circle motif (half light/half dark, symmetrical), centered. Include the label text ‘NEUTRAL’ in all caps within the design, legible.”

* (Shared style contract, override: text allowed)

[ ] **ICON SLIGHTLY EVIL- Alignment token icon: a subtle horned crescent or faint jagged shadow motif (less intense than EVIL), centered. Include the label text ‘SLIGHTLY EVIL’ in all caps within the design, legible.”

* (Shared style contract, override: text allowed)

[ ] **ICON EVIL- Alignment token icon: sharp horned crescent / spiked crown / infernal sigil motif (clearly ‘evil’), centered. Include the label text ‘EVIL’ in all caps within the design, legible.”

* (Shared style contract, override: text allowed)

---

### Clockwise / Anti-clockwise (2 icons)

[ ] **ICON CLOCKWISE- A simple clock face icon with a bold curved arrow indicating clockwise rotation around the clock. No text. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

[ ] **ICON ANTI-CLOCKWISE- A simple clock face icon with a bold curved arrow indicating anti-clockwise (counterclockwise) rotation around the clock. No text. Centered. Square icon, 1:1 aspect ratio. Clean game-token style. Bold silhouette, high contrast, minimal detail, centered composition. Subtle inner shadow + rim highlight for a tactile token feel. Transparent background (PNG). No border unless explicitly requested. No extra text unless explicitly requested. No watermark.

---

## B) Three Codex prompts for React components (tanstack + shadcn + tailwind)

These are written as **prompts you give Codex**. They assume you already have a `Token` component you can drop in.

[x] 
### Prompt 1 — n=1 (“YOU SEE:”)

“Create a reusable React + TypeScript component for a TanStack React app using Tailwind and shadcn/ui. Name it `TokenRevealOne`. It should accept props:

* `token: React.ReactNode` (this will be a <Token .../> element passed in)
* `className?: string`
  Render a compact card-like container (shadcn `Card`) with title text on top: `YOU SEE:` (all caps). Beneath it, center the single token horizontally. Spacing should be consistent and responsive. No Redux logic inside; purely presentational. Export default.”

---

[x] 
### Prompt 2 — n=2 (“YOU SEE:” with OR between tokens)

“Create a reusable React + TypeScript component for a TanStack React app using Tailwind and shadcn/ui. Name it `TokenRevealTwo`. Props:

* `leftToken: React.ReactNode`
* `rightToken: React.ReactNode`
* `className?: string`
  Render a `Card` with title `YOU SEE:` (all caps). Below, lay out the two tokens on one row, equally spaced. Put the text `OR` (all caps) centered between them (not overlapping tokens). Tokens should be visually aligned and equidistant. Purely presentational. Export default.”

---

[x]
### Prompt 3 — n=3 (“THESE CHARACTERS ARE OUT OF PLAY:” with AND between tokens)

“Create a reusable React + TypeScript component for a TanStack React app using Tailwind and shadcn/ui. Name it `TokenRevealThree`. Props:

* `tokens: [React.ReactNode, React.ReactNode, React.ReactNode]`
* `className?: string`
  Render a `Card` with title `THESE CHARACTERS ARE OUT OF PLAY:` (all caps). Below, display the three tokens in a row, equally spaced. Place the text `AND` (all caps) between token1 and token2, and also between token2 and token3. Ensure consistent alignment, spacing, and wrapping on small screens (stack vertically if needed, keeping AND between items). Purely presentational. Export default.”

---

That gives you **22 icon prompts + 3 Codex prompts**, all consistent and production-friendly.

If you want these icons to match your tarot cards even harder (same color palette / line weight), you can paste one extra line into every icon prompt: “match the visual style of the previously generated tarot execution deck: painterly realism, muted gothic palette, high contrast.”
